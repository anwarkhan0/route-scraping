version: '3.1'
services:
  routes_scrapes_db:
    image: mysql:5.7
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - 4000:3306
    environment:
      - MYSQL_ROOT_PASSWORD=login1234
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
  node-app:
    image: routes-scraper
    restart: always
    ports:
      - 3000:3000
    environment:
      - DB_HOST=routes_scrapes_db
      - DB_NAME=routes_scrapes
      - DB_USER=root
      - DB_PASS=cnsC61PPlRxOdh0
